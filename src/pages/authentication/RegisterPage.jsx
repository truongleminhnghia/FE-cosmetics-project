import React from 'react'
import { Button, Checkbox, Form, Input } from 'antd';
import FormActionComponent from '../../components/form/FormActionComponent';


const RegisterPage = () => {
    return (
        <FormActionComponent isLogin={false} />
    )
}

export default RegisterPage
